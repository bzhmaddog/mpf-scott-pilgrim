name: MPF Media Controller Front-end

on:
  push:
    branches: [main]
    paths:
      - 'media-controller/front/**'
      - '.github/workflows/ci-front.yml'
  pull_request:
    branches: [main]
    paths:
      - 'media-controller/front/**'
      - '.github/workflows/ci-front.yml'

permissions:
  contents: read

jobs:
  front-ci:
    name: Lint · Test · Coverage
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: media-controller/front

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: media-controller/front/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test & coverage
        run: npm run test:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v5
        if: always()
        with:
          name: coverage-report
          path: media-controller/front/coverage/
          retention-days: 14
