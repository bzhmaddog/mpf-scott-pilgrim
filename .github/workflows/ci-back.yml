name: MPF Media Controller Back-end

on:
  push:
    branches: [main]
    paths:
      - 'media-controller/back/**'
      - '.github/workflows/ci-back.yml'
  pull_request:
    branches: [main]
    paths:
      - 'media-controller/back/**'
      - '.github/workflows/ci-back.yml'

permissions:
  contents: read

jobs:
  back-ci:
    name: Lint · Test · Coverage
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: media-controller/back

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: media-controller/back/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test & coverage
        run: npm run test:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v5
        if: always()
        with:
          name: back-coverage-report
          path: media-controller/back/coverage/
          retention-days: 14
